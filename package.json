{
  "name": "anon-kiwi",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "eleventy --serve",
    "clean": "node -e \"require('fs').rmSync('_site',{ recursive: true, force: true })\"",
    "build": "eleventy && pagefind --site _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.0.0",
    "@picocss/pico": "2.1.1",
    "pagefind": "1.5.2"
  }
}
